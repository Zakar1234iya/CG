#pragma once
/**
 * D3dx8math.h - DEPRECATED
 * 
 * This header has been replaced by D3DXCompat.h which provides unified
 * cross-platform DX8 type definitions without Windows-only dependencies.
 * 
 * All code should migrate to using D3DXCompat.h instead.
 */

#include "D3DXCompat.h"

// Legacy typedefs for backward compatibility during migration
// New code should use the types from D3DXCompat.h directly
